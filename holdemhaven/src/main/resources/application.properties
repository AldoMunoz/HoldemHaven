spring.application.name=holdemhaven

# PostgreSQL Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://holdemhaven.claygsk26yqp.us-east-2.rds.amazonaws.com:5432/holdemhaven}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:HoldemHaven2024}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=${PORT:8080}